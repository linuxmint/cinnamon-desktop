#!/bin/sh

set -e

if [ -e /usr/bin/glib-compile-schemas ]; then
	if [ -e /usr/bin/cinnamon-desktop-migrate-mediakeys ]; then
	  /usr/bin/glib-compile-schemas /usr/share/glib-2.0/schemas/
	  /usr/bin/cinnamon-desktop-migrate-mediakeys
	fi
fi

exit 0
